
const SectionHeader = ({title}) => {
    // console.log(value)
    return (
        <div>
            {/* <h5>{value.sub_title}</h5> */}
            <h3>{title}</h3>
        </div>
    );
};

export default SectionHeader;